#!/usr/bin/env ruby
require_relative '../lib/opener/pos_tagger'

tokenizer = Opener::POSTagger.new

output, error, process = tokenizer.run(STDIN.read)

if process.success?
puts output

  STDERR.puts error unless error.empty?
else
  abort error
end
